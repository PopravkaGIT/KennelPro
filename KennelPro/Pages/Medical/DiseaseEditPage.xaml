<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:vm="clr-namespace:KennelPro.ViewModels.Medical" x:Class="KennelPro.Pages.Medical.DiseaseEditPage" x:DataType="vm:DiseaseEditViewModel" Title="{Binding PageTitle}"><ScrollView><VerticalStackLayout Padding="16" Spacing="16"><Label Text="Diagnosis" FontSize="16"/><Entry Text="{Binding Diagnosis}" FontSize="18" HeightRequest="52"/><Label Text="Symptoms" FontSize="16"/><Editor Text="{Binding Symptoms}" FontSize="18" HeightRequest="120"/><Label Text="Start date" FontSize="16"/><DatePicker Date="{Binding StartDate}" FontSize="18" HeightRequest="52"/><Label Text="Status" FontSize="16"/><Picker ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding SelectedStatus}" FontSize="18" HeightRequest="52"/><HorizontalStackLayout Spacing="12"><Switch IsToggled="{Binding HasRecoveryDate}"/><Label Text="Set recovery date" FontSize="16" VerticalOptions="Center"/></HorizontalStackLayout><DatePicker Date="{Binding RecoveryDate}" IsVisible="{Binding HasRecoveryDate}" FontSize="18" HeightRequest="52"/><Button Text="Save" Command="{Binding SaveCommand}" FontSize="18" HeightRequest="52"/><Button Text="Cancel" Command="{Binding CancelCommand}" BackgroundColor="Transparent" TextColor="{StaticResource Primary}" FontSize="16" HeightRequest="48"/></VerticalStackLayout></ScrollView></ContentPage>
